describe('Basic Test', () => {
  test('math - multiply', () => {
    expect(2 * 3).toBe(6);
  });
});

describe('Basic Test', () => {
  test('math - subtraction', () => {
    expect(4 - 3).toBe(1);
  });
});
